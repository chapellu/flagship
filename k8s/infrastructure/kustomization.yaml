apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - repository.yaml
  - release.yaml
  # cert-manager — issues the Let's Encrypt certificate consumed by the
  # shared Gateway (see ../infrastructure-config).
  - cert-manager-repository.yaml
  - cert-manager-release.yaml
