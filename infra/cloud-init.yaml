#cloud-config

package_update: true
package_upgrade: true

packages:
  - ufw
  - curl
  - ca-certificates
  - unattended-upgrades

runcmd:
  - ufw allow 22/tcp
  - ufw --force enable
  - systemctl enable unattended-upgrades
